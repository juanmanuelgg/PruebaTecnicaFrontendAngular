version: '3.8'

services:
  pokemon-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pokemon-app
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
